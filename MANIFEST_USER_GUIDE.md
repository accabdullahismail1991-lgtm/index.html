# دليل تشغيل نظام مانيفست

هذا دليل عملي لاستخدام نظام **مانيفست — منصة عمليات المطبخ المركزي**
(ملف `central-kitchen-prototype.html`)، موجّه لفريق العمليات: مديرة/مدير
العمليات، أمين المستودع، فريق المطبخ المركزي، مسؤولي الفروع، والسائقين.

> **ملاحظة مهمة قبل أي شيء:** الشاشات كلها شغّالة وتقدر تتنقل بينها
> وتجرّب كل الأزرار، لكن البيانات اللي شايفها فيها (الأصناف، التحويلات،
> الفروع، الأرقام...) **بيانات تجريبية ثابتة** مكتوبة داخل الملف نفسه،
> مش متصلة ببيانات حقيقية أو بقاعدة بيانات فعلية لسه. فيه شغل شغّال في
> الخلفية (`manifest-backend/`) لبناء القاعدة الحقيقية اللي هتخلي كل
> حركة (تحويل، إنتاج، استلام...) تتسجل فعليًا وتفضل محفوظة — لسه محتاج
> مشروع Firebase حقيقي عشان يشتغل ويترتبط بالشاشات دي. لحد ما ده يحصل،
> اعتبر الشاشات دي **عرض تجريبي كامل لشكل النظام** مش نظام تشغيلي فعلي.

## الدخول على النظام

النظام ملف واحد يفتح مباشرة من المتصفح (`central-kitchen-prototype.html`)،
من غير تسجيل دخول فعلي حاليًا — بيفتح على اسم مستخدم تجريبي ثابت
("سارة العامري — مديرة العمليات") ظاهر في أسفل القائمة الجانبية. لما
النظام يترتبط بالـ backend الحقيقي، هيبقى فيه تسجيل دخول فعلي بصلاحيات
مختلفة لكل دور (مدير، أمين مستودع، سائق، فرع...).

## القائمة الجانبية — كل الشاشات

القائمة الجانبية (على اليمين) فيها كل وحدات النظام، مقسّمة لمجموعات:

### 🏠 الرئيسية
الشاشة الأولى اللي بتفتح. فيها نظرة سريعة على كل حاجة في يوم واحد:
- 4 مؤشرات أعلى الشاشة: تحويلات مفتوحة، فروقات مفتوحة، توصيلات اليوم،
  قيمة الهالك الشهري.
- **شريط الحركة المباشر**: آخر الحركات اللي حصلت في المخزون لحظة بلحظة.
- **الموافقات المعلقة**: أي طلب محتاج موافقتك (تحويل بقيمة كبيرة مثلًا)
  قبل ما ينفّذ.
- **حالة الفروع**: نظرة سريعة على كل فرع.
- **آخر التحويلات**: آخر التحويلات بين المخازن والفروع.

### 📊 لوحات المعلومات
تقارير واتجاهات مجمّعة: قيمة المخزون حسب كل موقع، واتجاه الفروقات على
مدار آخر 30 يوم. الشاشة دي هتاخد قيمتها الحقيقية أول ما ترتبط بتقارير
"السناب شوت" اللي اتبنت في الـ backend (راجع `manifest-backend/DATA_MODEL.md`).

### 📒 دفتر المخزون
**السجل الأساسي اللي كل حاجة في النظام بترجع له.** كل حركة مخزون —
استلام، تحويل، إنتاج، هالك، تعديل يدوي — بتتسجل هنا بالوقت والصنف
والموقع والكمية، وثابتة لا تتغيّر ولا تُحذف أبدًا (ده نفس مبدأ "دفتر
الأستاذ" — تصحيح أي خطأ بيبقى بقيد جديد يعدّل الرصيد، مش بمسح القيد
القديم). فيه زرار "تعديل يدوي" لتسجيل تصحيح استثنائي بسبب موثّق.

### 🏬 المستودع
شاشة المستودع المركزي: الأصناف المخزّنة، الأصناف اللي وصلت لحد إعادة
الطلب ولازم تتطلب، نسبة إشغال الأرفف، والدفعات اللي هتنتهي صلاحيتها
قريبًا. فيها زرار لتسجيل إيصال استلام بضاعة جديد.

### 🍞 المطبخ المركزي
شاشة تشغيلات الإنتاج: كل تشغيلة إنتاج، الوصفة (BOM) المستخدمة، عدد
الدفعات، الناتج، وحالتها. تقدر تجدول تشغيلة إنتاج جديدة من هنا. تحت
الجدول، مثال لوصفة كاملة (خبز العجين المخمّر) بيوضّح مكوّناتها والكمية
المطلوبة لكل دفعة ونسبة الفاقد المسموح بها — ده بالظبط اللي أمر
الإنتاج في الـ backend (`production.js`) بيسحبه من المخزون تلقائيًا
لما التشغيلة تبدأ، ويسجّل الناتج الفعلي لما تخلص (مع رصد أي فرق بين
المخطط والفعلي).

### 🏪 الفروع
كل الفروع النشطة، بحالتها الحالية. فيه زرار لإضافة فرع جديد.

### 🔁 التحويلات
كل تحويل بضاعة بين المستودع/المطبخ المركزي/الفروع: من، إلى، الأصناف،
الحالة، ومقدّم الطلب. فيه زرار لعمل تحويل جديد. **ده أهم شاشة بترتبط
مباشرة بالـ backend الحقيقي** — دورة التحويل هناك (تقديم ← اعتماد ←
شحن ← استلام) مبنية بالكامل وجاهزة، بما فيها معالجة حالة "وصل جزء من
البضاعة بس" بشكل صحيح (الفرق بيفضل ظاهر في "الترانزيت" ويتسجّل كفرق،
مش بيختفي).

### 🚚 التوصيل
تتبّع السائقين والمسارات لحظيًا (خريطة + جدول). ده جزء لسه مبنيش في
الـ backend (يحتاج تطبيق سائق فعلي بـ GPS حقيقي — راجع القيود المذكورة
في `manifest-backend/ARCHITECTURE_DECISIONS.md`).

### 📥 الاستلام
تأكيد استلام شحنة مقابل الكميات المتوقعة، مع تحديد أي فرق (تالف/ناقص)
لكل صنف، وزرار لتأكيد الاستلام أو رفعه للمراجعة.

### 🔢 الجرد
جرد دوري أو شامل لفرع معيّن: مقارنة الكمية بالنظام مقابل الكمية
المجرودة فعليًا، وحساب الفرق تلقائيًا. هذا الجزء لسه مبنيش في
الـ backend (التحويلات والإنتاج مبنيين، الجرد هو التالي في الخطة).

### ⚠️ محرّك الفروقات
كل الفروقات (المتوقع مقابل الفعلي) من أي مصدر — تحويل، إنتاج، جرد،
هالك — في جدول واحد، مع درجة الخطورة والحالة. في الـ backend، هذا
مبني فعليًا لفروقات التحويل والإنتاج، ولسه محتاج بناء لفروقات الجرد
والهالك.

### ✅ الموافقات
أي طلب (تحويل، صرف...) تجاوز حد قيمة معيّن ومحتاج موافقة قبل التنفيذ.
في الـ backend، حدود الموافقة دي **بيانات قابلة للتعديل** (جدول
`approvalRules`) مش أرقام مكتوبة في الكود — يعني تقدر تغيّر الحد بدون
حاجة لتعديل برمجي.

### 🗑️ الهالك
تسجيل أي بضاعة تالفة أو منتهية الصلاحية أو مرمية، مع القيمة المفقودة.

### 👥 المستخدمون والصلاحيات
إدارة من له حق الدخول على النظام، ودوره (مدير، أمين مستودع، سائق...)،
وإيه اللي مسموح له يعمله بالظبط. في الـ backend، الصلاحيات دي بتتحدد
عن طريق دور كل مستخدم (`roles`) ومفروضة من السيرفر نفسه — مش مجرد
إخفاء زرار في الشاشة، يعني حتى لو حد حاول يتحايل من المتصفح مش هيقدر
ينفّذ حاجة مش مسموح له بيها.

### 🕵️ سجل التدقيق
سجل كامل لكل حركة تغيير حصلت في النظام: مين عملها، إمتى، وإيه اللي
اتغيّر بالظبط (قبل/بعد). سجل ثابت لا يُعدَّل ولا يُحذف — مرجع أساسي
لأي مراجعة مالية أو تشغيلية لاحقًا.

## الخطوات القادمة

1. إنشاء مشروع Firebase حقيقي وربطه بالكود الموجود في `manifest-backend/`.
2. ربط شاشات النظام دي بالبيانات الحقيقية بدل البيانات التجريبية،
   شاشة بشاشة (التحويلات والمطبخ المركزي جاهزين للربط أول لأن الجزء
   الخاص بيهم في الـ backend مبني بالفعل).
3. تفعيل تسجيل دخول حقيقي بصلاحيات فعلية لكل دور.
4. استكمال باقي الوحدات في الـ backend (الجرد، لوحات المعلومات
   الحقيقية، مركز الإشعارات) بنفس الترتيب اللي في
   `manifest-backend/PROJECT_STATUS.md`.
